import React from 'react';

const ImageTooltip = ({ children }) => {
    return (
        <div style={{ position: "relative" }}>
            {children}
        </div>
    );
}

export default ImageTooltip;
